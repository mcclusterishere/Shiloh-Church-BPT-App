{
  "name": "shiloh-native",
  "private": true,
  "version": "0.1.0",
  "description": "Capacitor shell that wraps the Shiloh PWA for the Apple App Store and Google Play. Read native/README.md first, then docs/APP-STORE.md for the whole store path.",
  "scripts": {
    "sync": "rsync -a --delete --exclude=/native --exclude=/.git --exclude=/.github --exclude=/docs --exclude=/scripts --exclude=/supabase --exclude=node_modules --exclude=.DS_Store --exclude='*.log' ../ www/"
  },
  "devDependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  }
}
